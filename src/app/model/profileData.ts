import { Byte } from "@angular/compiler/src/util";

export interface ProfileData {
    id: number;
    email: string;
    firstName: string;
    lastName: string;
    nickname: string;
    phoneNumber: number;
    profilePicture: any;
}